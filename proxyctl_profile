#
# When both protocols are enabled, 
# http will be used for http proxy environments (e.g. http_proxy)
# and socks will be used for anything else (e.g. ftp_proxy)
#
# SOCKS Proxy Settings
## [REQUIRED]
SOCKS_ENABLE=true     

## [OPTIONAL] Default is 127.0.0.1
SOCKS_HOST="10.0.0.5" 

## [OPTIONAL] Default is 10808
SOCKS_PORT="1080"     

## [OPTIONAL] Default is socks5h
SOCKS_VERSION="socks5h"


# HTTP Proxy Settings
## [REQUIRED] 
HTTP_ENABLE=true

## [OPTIONAL] Default is 127.0.0.1
HTTP_HOST="192.168.1.50"

## [OPTIONAL] Default is 10808
HTTP_PORT="3128" 

# vim: ft=sh:
